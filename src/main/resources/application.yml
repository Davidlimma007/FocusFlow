spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/focusflow_db
    username: focusflow
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true